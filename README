# Parimal Khanpara




# 1
print('Parimal Khanpara')
# 2
user_name=input("Enter your name :")
print('Hello', user_name)
# 3
x=int(input("enter the first number :"))
y=int(input("enter the second number :"))
Ans=x+y
print("Sum :", Ans)
# 4

y=100
o=29

ab=(y+o)*3

Exponent_result=ab**2
print('the result of the calculation was:' ,Exponent_result)
# 5

name=input("Enter your name: ")
year_of_birth=int(input("Enter your year of birth: "))
age=int(input("Enter your age: "))

last_two_digits=str(year_of_birth)[-2:]
first_three_letters= name[:3]
age_power=age**2
Password=last_two_digits+first_three_letters+str(age_power)
print("Password:", Password)
# 6

p = int(input("Enter the first number: "))
k = int(input("Enter the second number: "))

if p % 2 == 0 and k % 2 == 0:
    print("Both numbers are even.")

elif p % 2 == 0 or k % 2 == 0:
    print("One of the numbers is even.")

else:
    print("Both numbers are odd.")

# 7
​
x = int(input("Give me your no(1to9): "))
total_sum = 0
for i in range(x):
    total_sum += i
print("The sum was:", total_sum)
# 8

import random

def guessing_game():

    secret_number = random.randint(0, 10)
    
    tries = 0
    
    while True:
        guess = int(input("Player: "))
        tries += 1

        if guess == secret_number:
            print("That's right! Number of tries:", tries)
            break
        elif guess < secret_number:
            print("Try a greater number.")
        else:
            print("Try a smaller number.")
            
print("Welcome to the Guessing Game!")
guessing_game()
# 9

import random

def guessing_game(player):

    secret_number = random.randint(0, 10)
    
    tries = 0
    
    while True:
        guess = int(input(f"Player {player}: "))

        tries += 1

        if guess == secret_number:
            print(f"That's right! Number of tries: {tries}")
            return tries
        elif guess < secret_number:
            print("Try a greater number.")
        else:
            print("Try a smaller number.")
            
print("Welcome to the Guessing Game!")

player1_tries = guessing_game(1)

player2_tries = guessing_game(2)

if player1_tries < player2_tries:
    print("Winner is Player 1")
elif player1_tries > player2_tries:
    print("Winner is Player 2")
else:
    print("It's a tie!")
# 9
p=lambda a,b:a*b
a=5
b=6
print(a*b)
# 11
import math
x=10
area=math.pi*x*x
print('area is circle is:',area)
